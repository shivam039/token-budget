{
  "name": "token-budget-monorepo",
  "private": true,
  "type": "module",
  "description": "Monorepo for token-budget core and its adapter/tokenizer packages.",
  "workspaces": [
    "packages/*",
    "bench",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@shivam.dixit/token-budget --workspace=@shivam.dixit/token-budget-tiktoken --if-present && npm run build --workspaces --if-present",
    "test": "vitest run && npm run test --workspaces --if-present",
    "test:coverage": "npm run test:coverage --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "bench": "npm run bench --workspace=token-budget-bench",
    "guardrails": "node scripts/guardrails/runner.js",
    "generate:dataset": "tsx scripts/generate-context-dataset.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
