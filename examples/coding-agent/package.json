{
  "name": "example-coding-agent",
  "private": true,
  "version": "0.0.0",
  "description": "A coding-agent loop (system instructions + tool calls + tool results) kept inside its token budget, with tool-call/tool-result pairs guaranteed atomic.",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "token-budget": "^0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
